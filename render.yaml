services:
  - type: web
    name: abpharma
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm --prefix ../frontend install --include=dev && npm --prefix ../frontend run build
    startCommand: npm start
    envVars:
      - fromGroup: abpharma-env
